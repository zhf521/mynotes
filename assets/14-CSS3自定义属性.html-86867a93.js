import{_ as s,o as n,c as a,b as e}from"./app-e62fd022.js";const c={},o=e(`<p>CSS3自定义属性是一种定义在 <code>:root</code> 参照根元素上的属性，可以被所有后代元素所继承和使用</p><p>自定义属性可以用 <code>--</code> 作为前缀来定义，如：<code>--primary-color: #ff0000</code></p><p>定义自定义属性后，可以在任何CSS规则内使用<code>var()</code>函数来引用自定义属性的值</p><p>示例：</p><p>定义一个自定义属性：</p><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token selector">:root</span> <span class="token punctuation">{</span>
  <span class="token property">--primary-color</span><span class="token punctuation">:</span> #ff0000<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>使用自定义属性：</p><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token selector">body</span> <span class="token punctuation">{</span>
  <span class="token property">background-color</span><span class="token punctuation">:</span> <span class="token function">var</span><span class="token punctuation">(</span>--primary-color<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在上述代码中，我们定义了一个名为 <code>--primary-color</code> 的自定义属性，并将其值设置为红色。在 <code>body</code> 元素的CSS规则中，我们使用 <code>var()</code> 函数来引用这个自定义属性，并将其值设置为 <code>background-color</code> 的值，这样 <code>body</code> 的背景色会使用 <code>--primary-color</code> 自定义属性的值，即红色</p><p>使用自定义属性的好处是代码的可重用性和构建更易于维护的样式库，可以使开发者在整个网站中更容易地应用相同的样式属性</p>`,10),p=[o];function t(d,r){return n(),a("div",null,p)}const i=s(c,[["render",t],["__file","14-CSS3自定义属性.html.vue"]]);export{i as default};
